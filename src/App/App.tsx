import React, { FC } from 'react';
import { ThemeName } from '../themes/types'

export type Configutation = {
    theme: ThemeName;
    bestScore: number;
    rows: number;
    cols: number;
}

const APP_NAME = '@)$*'

const App: FC = () => {

    


    return <div>1</div>
}

export default App;